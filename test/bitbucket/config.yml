core:
  alerter:
    delay: 0
  metrics:
    elasticsearch:
      hosts: [localhost]
    redis:
      host: localhost
  redis:
    nonpersistent:
      host: localhost
    persistent:
      host: localhost
      port: 6379

datastore:
  hosts: [localhost]

logging:
  log_as_json: false
  log_to_console: true
  log_to_file: false
  log_to_syslog: false