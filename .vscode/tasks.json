{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Docker Compose - Pytest",
            "type": "shell",
            "command": "docker-compose up",
            "options": {
                "cwd": "${workspaceFolder}/test",
            },
            "problemMatcher": [],
            "runOptions": {
                "instanceLimit": 1
            }
        }
    ]
}
